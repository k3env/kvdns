kind: pipeline
name: default
type: kubernetes
steps:
  - name: build
    image: plugins/docker
    settings:
      username: k3env
      password:
        from_secret: DOCKER_PASSWORD
      repo: k3env/kvdns
      tags:
        - ${DRONE_COMMIT_SHA}
        - latest
trigger:
  event:
    - push
---
kind: pipeline
name: default
type: kubernetes
steps:
  - name: build
    image: plugins/docker
    settings:
      username: k3env
      password:
        from_secret: DOCKER_PASSWORD
      repo: k3env/kvdns
      tags:
        - ${DRONE_TAG}
        - stable
trigger:
  event:
    - tag
